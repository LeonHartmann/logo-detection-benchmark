rungs: [1080, 720, 480, 240, 144]
jpeg_quality: 85
max_tokens: 4000  # reasoning-tier models can burn completion tokens; 2000 truncated one busy frame in smoke
timeout_s: 180
max_retries: 4
concurrency:
  dashscope: 12
  openai: 12
  openrouter: 16  # hosts 5 of the 12 models; the bottleneck at 8
  anthropic: 12
brands_file: brands/delay.yaml
